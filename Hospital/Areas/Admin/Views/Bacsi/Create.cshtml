@model Hospital.Models.BacSi

@{
    ViewData["Title"] = "Thêm Bác sĩ mới";
    // Ép kiểu ViewData sang SelectList để kiểm tra
    var userList = (SelectList)ViewData["IdentityUserId"];
}

<form method="post" enctype="multipart/form-data">
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Thêm Bác sĩ</h2>
            <hr />
        </div>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="row">
            <div class="col-12">

                @* TRƯỜNG: IdentityUserId (Tài khoản liên kết) - Dùng Dropdown *@
                <div class="mb-3">
                    <label asp-for="IdentityUserId" class="form-label">Tài khoản liên kết (Email)</label>
                    @if (userList != null && userList.Any())
                    {
                        <select asp-for="IdentityUserId" asp-items="@userList" class="form-select">
                            <option disabled selected>-- Chọn tài khoản --</option>
                        </select>
                        <span asp-validation-for="IdentityUserId" class="text-danger"></span>
                    }
                    else
                    {
                        <p class="text-danger">Không có tài khoản người dùng nào chưa có hồ sơ Bác sĩ để liên kết.</p>
                        <p class="text-muted">Hãy tạo tài khoản mới trước khi tạo hồ sơ Bác sĩ.</p>
                    }
                </div>

                @* TRƯỜNG: HoTen *@
                <div class="mb-3">
                    <label asp-for="HoTen"></label>
                    <input asp-for="HoTen" class="form-control" />
                    <span asp-validation-for="HoTen" class="text-danger"></span>
                </div>

                @* TRƯỜNG: ChuyenMon *@
                <div class="mb-3">
                    <label asp-for="ChuyenMon"></label>
                    <input asp-for="ChuyenMon" class="form-control" />
                    <span asp-validation-for="ChuyenMon" class="text-danger"></span>
                </div>

                @* TRƯỜNG: BangCap *@
                <div class="mb-3">
                    <label asp-for="BangCap"></label>
                    <input asp-for="BangCap" class="form-control" />
                </div>

                @* TRƯỜNG: MoTaChiTiet *@
                <div class="mb-3">
                    <label asp-for="MoTaChiTiet"></label>
                    <textarea asp-for="MoTaChiTiet" rows="5" class="form-control"></textarea>
                </div>

                @* TRƯỜNG: HinhAnhUrl (File Upload) *@
                <div class="mb-3">
                    <label for="file" class="form-label">Ảnh Đại diện</label>
                    <input type="file" name="file" id="file" class="form-control" />
                </div>

                @* TRƯỜNG: IsActive *@
                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" asp-for="IsActive">
                    <label class="form-check-label" asp-for="IsActive"></label>
                </div>

                <button type="submit" class="btn btn-primary" style="width:150px">Thêm Bác sĩ</button>
                <a asp-controller="BacSi" asp-action="Index" class="btn btn-secondary" style="width:150px">
                    Quay lại Danh sách
                </a>

            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}